// Clears the primary velocity grid and the linked list grid.

#version 450

#include "bindings_update_particles_and_grid.glsl"

COMPUTE_PASS_VOLUME

void main() { imageStore(LinkedListDualGrid, ivec3(gl_GlobalInvocationID), uvec4(0)); }